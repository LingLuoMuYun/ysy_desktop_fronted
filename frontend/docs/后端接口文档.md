# 衍生桌面智算后端 API 接口说明文档
状态：新版原型功能模块对齐冻结版  
原型地址：`http://10.0.195.51:5175/`  
关联功能拆分：`docs/prd/feature-breakdown.md`  
说明：本文档按新版产品功能模块组织后端 API。每个接口均包含前置条件、说明和接口说明。本文档已对齐前端页面一级导航：首页、项目、任务、数据、模型、设置，以及任务页的创建训练任务、创建部署任务、任务概览、配置参数、日志与诊断、输出结果等操作入口。

## 0. 全局约定
### 0.1 基础约定
#### 1. 前置条件
+ **业务阶段**：桌面端 MVP 后端 API 契约设计。
+ **前置动作**：前端已切换为新版原型，一级导航为：首页、项目、任务、数据、模型、设置。
+ **基础路径**：所有业务接口默认使用 `/api` 前缀。

#### 2. 说明
+ 新版原型中，训练任务和部署服务统一归入“任务”模块。
+ 数据集和模型产物升级为独立资产模块。
+ 前端只提交结构化参数，不直接提交任意 shell 命令。
+ 本地路径、进程启动、环境修复、删除、清理等操作必须由后端做安全校验。
+ 高风险操作必须传入 `confirmed: true`。
+ 所有业务接口的实际响应必须使用统一响应包裹；下文示例中的“输出”若未显式写出 `success/data/error/requestId`，均表示 `data` 内部的业务载荷。
+ 创建、启动、停止、生成报告等可能被重复点击或重试的接口支持 `Idempotency-Key` 请求头；也可在请求体中传入 `idempotencyKey`。同一幂等键在同一业务资源范围内必须返回同一业务结果或明确的幂等冲突错误。

#### 3. 接口说明
**统一成功输出**

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `success` | `Boolean` | 固定为 `true` |
| `data` | `Object / Array / Null` | 业务数据 |
| `error` | `Null` | 成功时为空 |
| `requestId` | `String` | 请求追踪 ID |

**统一成功输出示例**

```json
{
  "success": true,
  "data": {},
  "error": null,
  "requestId": "req-20260702-0001"
}
```


**统一失败输出**

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `success` | `Boolean` | 固定为 `false` |
| `data` | `Null` | 失败时为空 |
| `error.code` | `String` | 错误码 |
| `error.message` | `String` | 错误说明 |
| `error.detail` | `Object` | 错误详情 |
| `error.suggestion` | `String` | 下一步建议 |
| `requestId` | `String` | 请求追踪 ID |

**统一失败输出示例**

```json
{
  "success": false,
  "data": null,
  "error": {
    "code": "validation_error",
    "message": "参数校验失败",
    "detail": {},
    "suggestion": "请检查必填字段"
  },
  "requestId": "req-20260702-0002"
}
```


**通用错误码**

| 错误码 | 说明 |
| :--- | :--- |
| `unauthorized` | 未登录或 token 无效 |
| `validation_error` | 参数校验失败 |
| `not_found` | 资源不存在 |
| `path_not_found` | 本地路径不存在 |
| `path_not_allowed` | 路径超出允许范围 |
| `environment_not_ready` | 运行环境不可用 |
| `dataset_invalid` | 数据集格式或标注校验失败 |
| `model_invalid` | 模型文件或配置不可用 |
| `process_start_failed` | 本地任务进程启动失败 |
| `task_not_cancellable` | 当前任务不可停止 |
| `confirm_required` | 高风险操作缺少确认 |
| `idempotency_conflict` | 幂等键对应的历史请求与本次请求体不一致 |
| `future_not_supported` | 当前 MVP 暂不支持的高风险扩展能力 |
| `internal_error` | 后端内部错误 |


**分页输入**

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `keyword` | `String` | 搜索关键字 |
| `page` | `Integer` | 页码，默认 `1` |
| `pageSize` | `Integer` | 每页数量，默认 `20` |
| `sortBy` | `String` | 排序字段 |
| `sortOrder` | `String` | `asc` 或 `desc` |


**分页输出**

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `items` | `Array` | 当前页数据 |
| `page` | `Integer` | 当前页码 |
| `pageSize` | `Integer` | 每页数量 |
| `total` | `Integer` | 总数量 |
| `totalPages` | `Integer` | 总页数 |
| `hasMore` | `Boolean` | 是否还有下一页 |


### 0.2 高风险操作
#### 1. 前置条件
+ **业务阶段**：删除、停止、修复、清理。
+ **前置动作**：前端已展示确认弹窗，用户明确确认。

#### 2. 说明
以下接口必须在请求体中传入 `confirmed: true`。前端需要展示确认摘要；后端必须自行校验确认字段，不能只依赖前端弹窗。

#### 3. 接口说明
**统一高风险确认请求**

```json
{
  "confirmed": true,
  "confirmText": "可选，用户二次确认文本",
  "confirmToken": "可选，由预览接口或后端生成",
  "riskSummary": "前端展示用风险摘要"
}
```

缺少 `confirmed: true` 时返回：

```json
{
  "success": false,
  "data": null,
  "error": {
    "code": "confirm_required",
    "message": "高风险操作缺少用户确认",
    "detail": { "requiredField": "confirmed" },
    "suggestion": "请展示确认弹窗，用户确认后重试"
  },
  "requestId": "req-confirm-001"
}
```

| 操作 | 接口 |
| :--- | :--- |
| 清理本地垃圾文件 | `POST /api/system/cleanup` |
| 删除项目 | `DELETE /api/projects/{projectId}` |
| 删除任务 | `DELETE /api/tasks/{taskId}` |
| 启动任务 | `POST /api/tasks/{taskId}/start` |
| 停止任务 | `POST /api/tasks/{taskId}/stop` |
| 删除数据集 | `DELETE /api/datasets/{datasetId}` |
| 删除模型 | `DELETE /api/models/{modelId}` |
| 修复环境 | `POST /api/environments/{envId}/repair` |
| 删除环境 | `DELETE /api/environments/{envId}` |

删除项目、任务、数据集、模型、环境等记录时，MVP 默认只删除登记记录，不删除本地文件。请求中的 `deleteLocalFiles` 只允许为 `false`；传入 `true` 时返回 `future_not_supported` 或 `validation_error`。未来若支持删除本地文件，必须先经过清理预览接口并再次高风险确认。

### 0.3 通用检查结果结构
#### 1. 前置条件
+ **业务阶段**：数据检查、模型检查、环境检测、任务预检、推理服务兼容性检查。
+ **前置动作**：用户触发检查，或导入/创建后自动检查。

#### 2. 说明
数据、模型、环境、任务预检和部署兼容性统一使用 `CheckResult`，便于前端复用检查结果组件。

#### 3. 接口说明
**CheckResult**

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `status` | `String` | `pass`、`warning`、`failed`、`checking` |
| `summary` | `String` | 检查摘要 |
| `items` | `Array` | 检查项列表 |
| `items[].name` | `String` | 检查项名称 |
| `items[].status` | `String` | `pass`、`warning`、`failed`、`skipped` |
| `items[].severity` | `String` | `info`、`warning`、`error` |
| `items[].code` | `String` | 结构化问题码 |
| `items[].message` | `String` | 问题说明 |
| `items[].suggestion` | `String` | 建议动作 |
| `items[].blocking` | `Boolean` | 是否阻断创建或启动 |
| `items[].value` | `String / Number / Object / Null` | 检查值 |
| `items[].evidence` | `Array` | 证据列表，如文件路径、版本号、日志片段或检测值 |
| `checkedAt` | `String` | 最近检查时间 |
| `expiresAt` | `String` | 结果过期时间，可选 |
| `source` | `String` | `dataset`、`model`、`environment`、`task_preflight`、`deployment_compatibility` |
| `warnings` | `Array` | 非阻断警告 |

**示例**

```json
{
  "status": "warning",
  "summary": "存在 1 个非阻断警告，可继续创建但启动前需再次确认",
  "items": [
    {
      "name": "数据集检查",
      "status": "warning",
      "severity": "warning",
      "code": "LABEL_FILE_MISSING",
      "message": "发现 3 个标签文件缺失",
      "suggestion": "补齐标签或在训练前排除对应样本",
      "blocking": false,
      "value": 3,
      "evidence": ["labels/train/0001.txt", "labels/train/0042.txt"]
    }
  ],
  "checkedAt": "2026-07-02T15:30:00+08:00",
  "expiresAt": "2026-07-02T16:30:00+08:00",
  "source": "task_preflight",
  "warnings": ["3 个标签文件缺失"]
}
```


## 1. 首页
### 1.1 查询首页工作台
#### 1. 前置条件
+ **业务阶段**：用户进入首页。
+ **前置动作**：后端服务已启动；系统可读取本机资源和最近操作记录。

#### 2. 说明
返回首页所需的本机资源、最近操作和当前项目。

#### 3. 接口说明
**接口地址**

`GET /api/home/workbench`

**输入**

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `currentProjectId` | `String` | 当前项目 ID，可选 |
| `includeResources` | `Boolean` | 是否返回资源信息 |
| `recentLimit` | `Integer` | 最近操作数量 |


**输出**

```json
{
  "currentProject": {
    "id": "proj-defect",
    "name": "工业缺陷检测实验"
  },
  "resources": {
    "cpu": "48%",
    "gpu": "36%",
    "memory": "62%",
    "diskAvailable": "128GB",
    "networkDown": "3.2MB/s",
    "networkUp": "0.7MB/s"
  },
  "recentActions": []
}
```

### 1.2 查询本机资源
#### 1. 前置条件
+ **业务阶段**：首页右侧本机资源卡片。
+ **前置动作**：后端具备读取 CPU、GPU、内存、磁盘、网络状态的权限。

#### 2. 说明
用于实时展示 CPU、GPU、内存、磁盘和网络占用。

#### 3. 接口说明
**接口地址**

`GET /api/system/resources`

**输入**

无

**输出**

```json
{
  "cpu": { "usagePercent": 48 },
  "gpu": { "usagePercent": 36, "memoryUsed": "8.6GB", "memoryTotal": "24GB" },
  "memory": { "usagePercent": 62, "used": "39.7GB", "total": "64GB" },
  "disk": [
    { "id": "C", "available": "128GB", "usedPercent": 68 },
    { "id": "D", "available": "612GB", "usedPercent": 42 }
  ],
  "network": { "download": "3.2MB/s", "upload": "0.7MB/s" }
}
```

### 1.3 查询最近操作
#### 1. 前置条件
+ **业务阶段**：首页最近操作卡片。
+ **前置动作**：系统已记录项目、任务、数据、模型、环境相关操作。

#### 2. 说明
返回最近操作列表，供用户点击后让 AI 生成下一步计划。

#### 3. 接口说明
**接口地址**

`GET /api/activity/recent`

**输入**

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `limit` | `Integer` | 返回数量 |
| `projectId` | `String` | 项目 ID，可选 |


**输出**

```json
{
  "items": [
    {
      "id": "ra-1",
      "name": "训练失败：YOLOv8 缺陷检测训练",
      "time": "12 分钟前",
      "tone": "danger",
      "resourceType": "task",
      "resourceId": "task-yolo-failed"
    }
  ]
}
```

### 1.4 预览本地清理
#### 1. 前置条件
+ **业务阶段**：首页垃圾清理分析。
+ **前置动作**：用户点击“垃圾清理”或 AI 生成清理建议。

#### 2. 说明
仅分析可清理文件，不直接删除。

#### 3. 接口说明
**接口地址**

`POST /api/system/cleanup/preview`

**输入**

```json
{
  "scope": "all",
  "projectId": "proj-defect"
}
```

**输出**

```json
{
  "releasableSize": "5.7GB",
  "items": [
    { "path": "D:/workspace/defect/runs/tmp", "size": "2.1GB", "type": "training_cache" }
  ],
  "warnings": ["将保留 best.pt、最新日志和导出报告"]
}
```

### 1.5 执行本地清理
#### 1. 前置条件
+ **业务阶段**：首页垃圾清理执行。
+ **前置动作**：用户已确认清理路径清单。

#### 2. 说明
高风险操作。必须校验 `confirmed: true` 和路径白名单。

#### 3. 接口说明
**接口地址**

`POST /api/system/cleanup`

**输入**

```json
{
  "confirmed": true,
  "paths": ["D:/workspace/defect/runs/tmp"]
}
```

**输出**

```json
{
  "cleanedSize": "2.1GB",
  "cleanedCount": 128,
  "skipped": []
}
```

### 1.6 打开本地工具
#### 1. 前置条件
+ **业务阶段**：顶部本地工具启动器。
+ **前置动作**：用户选择 VS Code、Visual Studio、Cursor、File Explorer、Terminal 或 WSL。

#### 2. 说明
打开本地工具，并将当前项目目录作为上下文。

#### 3. 接口说明
**接口地址**

`POST /api/local-tools/open`

**输入**

```json
{
  "tool": "VS Code",
  "projectId": "proj-defect",
  "path": "D:/workspace/defect-detection"
}
```

**输出**

```json
{
  "opened": true,
  "tool": "VS Code",
  "path": "D:/workspace/defect-detection"
}
```

## 2. 项目
### 2.1 查询项目列表
#### 1. 前置条件
+ **业务阶段**：项目列表页。
+ **前置动作**：项目数据已初始化。

#### 2. 说明
支持搜索、项目类型筛选、状态筛选和分页。

#### 3. 接口说明
**接口地址**

`GET /api/projects`

**输入**

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `keyword` | `String` | 项目名称、类型、工作区搜索 |
| `type` | `String` | 项目类型 |
| `status` | `String` | 项目状态 |
| `page` | `Integer` | 页码 |
| `pageSize` | `Integer` | 每页数量 |


**输出**

```json
{
  "items": [
    {
      "id": "proj-defect",
      "name": "工业缺陷检测实验",
      "type": "目标检测",
      "status": "存在风险",
      "workspace": "D:/workspace/defect-detection",
      "datasetIds": ["data-defect"],
      "environmentId": "env-yolo",
      "updatedAt": "今天 10:25"
    }
  ],
  "page": 1,
  "pageSize": 20,
  "total": 1
}
```

### 2.2 查询项目规格
#### 1. 前置条件
+ **业务阶段**：创建项目弹窗。
+ **前置动作**：用户点击创建项目。

#### 2. 说明
返回项目类型、状态、可选代码来源、默认路径、可兼容环境。

#### 3. 接口说明
**接口地址**

`GET /api/projects/specs`

**输出**

```json
{
  "projectTypes": ["图像分类", "目标检测", "语义分割", "自定义深度学习", "LLM 微调", "Embedding 微调", "Rerank 微调", "自定义项目"],
  "codeSources": ["系统框架模板", "本地代码目录", "空项目"],
  "workspaceOptions": ["D:/workspace/defect-detection", "E:/ai-projects/support-sft"]
}
```

### 2.3 创建项目
#### 1. 前置条件
+ **业务阶段**：创建项目。
+ **前置动作**：用户已填写基础信息、保存位置和资源绑定。

#### 2. 说明
创建项目并绑定数据集、运行环境。

#### 3. 接口说明
**接口地址**

`POST /api/projects`

**输入**

```json
{
  "name": "工业缺陷检测实验",
  "type": "目标检测",
  "description": "检测金属表面划痕、污渍和缺口",
  "workspace": "D:/workspace/defect-detection",
  "codeSource": "系统框架模板",
  "codePath": "D:/workspace/defect-detection/code",
  "dependencyFile": "environment.yaml",
  "datasetIds": ["data-defect"],
  "environmentId": "env-yolo"
}
```

**输出**

```json
{
  "id": "proj-defect",
  "name": "工业缺陷检测实验",
  "status": "准备中"
}
```

### 2.4 查询项目详情
#### 1. 前置条件
+ **业务阶段**：项目详情页。
+ **前置动作**：用户从项目列表选择项目或访问 `/projects/{projectId}`。

#### 2. 说明
返回项目基础信息和摘要统计。项目详情页中的数据集、任务记录、模型资产列表不通过项目详情接口内嵌返回，统一复用各资产列表接口，避免项目详情数据量过大。

项目详情页数据来源：

| 前端区域 | 接口 |
| :--- | :--- |
| 项目基础信息 | `GET /api/projects/{projectId}` |
| 关联数据集 | `GET /api/datasets?projectId={projectId}` |
| 任务记录 | `GET /api/tasks?projectId={projectId}` |
| 模型资产 | `GET /api/models?projectId={projectId}` |
| 默认运行环境 | `GET /api/environments/{envId}` |

#### 3. 接口说明
**接口地址**

`GET /api/projects/{projectId}`

**输出**

```json
{
  "id": "proj-defect",
  "name": "工业缺陷检测实验",
  "type": "目标检测",
  "status": "存在风险",
  "workspace": "D:/workspace/defect-detection",
  "codePath": "D:/workspace/defect-detection/code",
  "datasetCount": 1,
  "taskCount": 5,
  "modelCount": 2,
  "environmentId": "env-yolo",
  "summary": {
    "latestTaskStatus": "failed",
    "latestTaskName": "YOLOv8 缺陷检测训练",
    "riskCount": 1
  }
}
```

### 2.5 更新项目
#### 1. 前置条件
+ **业务阶段**：编辑项目。
+ **前置动作**：用户修改项目基础信息或资源绑定。

#### 2. 说明
支持局部更新项目字段。

#### 3. 接口说明
**接口地址**

`PATCH /api/projects/{projectId}`

**输入**

```json
{
  "name": "工业缺陷检测实验",
  "datasetIds": ["data-defect"],
  "environmentId": "env-yolo"
}
```

**输出**

返回更新后的项目对象。

### 2.6 删除项目
#### 1. 前置条件
+ **业务阶段**：项目列表删除项目。
+ **前置动作**：用户已确认删除。

#### 2. 说明
删除项目登记，并解除数据集、模型、任务关联。默认不删除本地文件。

#### 3. 接口说明
**接口地址**

`DELETE /api/projects/{projectId}`

**输入**

```json
{
  "confirmed": true,
  "deleteLocalFiles": false
}
```

**输出**

```json
{
  "deleted": true,
  "projectId": "proj-defect"
}
```

## 3. 任务
### 3.1 查询任务列表
#### 1. 前置条件
+ **业务阶段**：任务列表页。
+ **前置动作**：系统中已有训练任务或部署任务。

#### 2. 说明
统一返回训练任务和部署任务。前端任务页的“训练任务 / 部署任务”筛选对应 `kind`；状态筛选中的“等待配置、待启动、运行中、已完成、已停止、启动失败、失败、异常”对应 `status`；“环境检查中、数据检查中、模型检查中、模型加载中、服务启动中、验证评估中、导出产物中”等细分项对应 `phase`。

#### 3. 接口说明
**接口地址**

`GET /api/tasks`

**输入**

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `keyword` | `String` | 任务名称、类型、项目名称 |
| `kind` | `String` | `training` 或 `deployment` |
| `type` | `String` | 具体任务类型 |
| `projectId` | `String` | 项目 ID |
| `status` | `String` | 任务主状态，见 `9.3 任务状态` |
| `phase` | `String` | 任务执行阶段，见 `9.3 任务状态` |
| `page` | `Integer` | 页码 |
| `pageSize` | `Integer` | 每页数量 |


**输出**

```json
{
  "items": [
    {
      "id": "task-yolo-running",
      "kind": "training",
      "type": "目标检测训练",
      "name": "baseline-exp-20240510-001",
      "status": "running",
      "statusText": "运行中",
      "phase": "training",
      "phaseText": "baseline 训练",
      "projectId": "proj-defect",
      "datasetId": "data-defect",
      "environmentId": "env-yolo",
      "progress": { "current": 32, "total": 50, "label": "32/50" },
      "endpoint": null,
      "canStart": false,
      "canStop": true,
      "canClone": true,
      "canDelete": true
    }
  ],
  "page": 1,
  "pageSize": 20,
  "total": 1
}
```

### 3.2 查询任务规格
#### 1. 前置条件
+ **业务阶段**：创建训练任务或部署任务。
+ **前置动作**：用户点击创建任务、复制任务、重试任务或从模型创建部署。

#### 2. 说明
返回训练任务和部署任务的动态表单配置。前端点击“创建训练任务”“创建部署任务”“复制为新任务”“重试任务”或模型详情页“创建推理服务”时调用。

#### 3. 接口说明
**接口地址**

`GET /api/tasks/specs`

**输入**

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `kind` | `String` | `training` 或 `deployment` |
| `type` | `String` | 任务类型 |
| `projectId` | `String` | 项目 ID，可选 |
| `modelAssetId` | `String` | 模型资产 ID，可选 |


**输出**

```json
{
  "trainingTypes": ["图像分类训练", "目标检测训练", "语义分割训练", "自定义深度学习训练", "LLM 微调", "Embedding 微调", "Rerank 微调"],
  "deploymentTypes": ["CV 推理服务", "LLM vLLM 推理服务", "Embedding 推理服务", "Rerank 推理服务", "自定义推理服务"],
  "compatibleDeploymentTypes": ["LLM vLLM 推理服务"],
  "recommendedEnvironmentIds": ["env-vllm"],
  "defaultPort": 7860,
  "blockingReasons": [],
  "fields": []
}
```

当传入 `kind=deployment&modelAssetId=` 时，`compatibleDeploymentTypes`、`recommendedEnvironmentIds`、`defaultPort` 和 `blockingReasons` 必须返回。`blockingReasons` 复用 `CheckResult.items[]` 结构。

### 3.3 任务预检
#### 1. 前置条件
+ **业务阶段**：创建任务前。
+ **前置动作**：用户已选择项目、数据集或模型、运行环境和执行入口。

#### 2. 说明
训练任务检查数据集、环境、路径、依赖、显存。部署任务检查模型、环境、端口、服务路径。预检输入中的 `execution` 必须使用 `TaskExecutionSnapshot`，与创建任务保存的执行快照保持同构。

#### 3. 接口说明
**接口地址**

`POST /api/tasks/preflight`

**输入**

```json
{
  "kind": "training",
  "type": "目标检测训练",
  "projectId": "proj-defect",
  "datasetId": "data-defect",
  "environmentId": "env-yolo",
  "execution": {
    "source": "project_default",
    "codePath": "D:/workspace/defect-detection/code",
    "workdir": "D:/workspace/defect-detection",
    "commandTemplate": "ultralytics train --data {{datasetPath}} --project {{outputPath}}",
    "resolvedCommand": "ultralytics train --data D:/datasets/defect/data.yaml --project D:/workspace/defect-detection/runs",
    "args": [],
    "envVars": { "TASK_ID": "preview" },
    "outputPath": "D:/workspace/defect-detection/runs",
    "interpreterPath": "D:/DesktopAI/Environments/yolo/python.exe",
    "environmentId": "env-yolo",
    "dependencyFile": "environment.yaml",
    "configFile": "data.yaml",
    "timeoutSeconds": 0,
    "logRef": null,
    "artifactScanRules": [{ "type": "model", "pattern": "**/*.pt" }]
  }
}
```

**输出**

```json
{
  "status": "pass",
  "summary": "预检通过",
  "items": [
    { "name": "数据集检查", "status": "pass", "severity": "info", "code": "DATASET_OK", "message": "数据集可用", "suggestion": "", "blocking": false, "value": null },
    { "name": "环境检查", "status": "pass", "severity": "info", "code": "ENV_OK", "message": "运行环境可用", "suggestion": "", "blocking": false, "value": null },
    { "name": "输出路径检查", "status": "pass", "severity": "info", "code": "OUTPUT_PATH_OK", "message": "输出路径可写", "suggestion": "", "blocking": false, "value": null }
  ],
  "checkedAt": "2026-07-02T15:40:00+08:00",
  "source": "task_preflight",
  "warnings": []
}
```

### 3.4 创建任务
#### 1. 前置条件
+ **业务阶段**：创建训练任务或部署任务。
+ **前置动作**：预检通过或用户选择允许带警告创建。

#### 2. 说明
统一创建训练任务和部署任务。`kind=training` 进入训练 Adapter，`kind=deployment` 进入部署 Adapter。创建任务时必须保存不可变的执行快照，后续项目、环境或模型记录变化不得修改已创建任务的快照。

#### 3. 接口说明
**接口地址**

`POST /api/tasks`

**幂等**

支持 `Idempotency-Key` 请求头或请求体 `idempotencyKey`，用于防止前端重复点击“创建训练任务 / 创建部署任务”导致重复任务。

**TaskExecutionSnapshot 字段**

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `source` | `String` | `project_default`、`historical_task`、`local_script`、`custom_command` |
| `codePath` | `String` | 代码目录，可选 |
| `workdir` | `String` | 命令执行工作目录，必填 |
| `commandTemplate` | `String` | 命令模板，不允许前端提交任意未校验 shell 片段 |
| `resolvedCommand` | `String` | 后端解析模板后的最终命令，必须落库用于复现 |
| `args` | `Array` | 参数数组 |
| `envVars` | `Object` | 注入环境变量 |
| `outputPath` | `String` | 输出目录 |
| `interpreterPath` | `String` | 任务绑定环境的 Python 解释器路径 |
| `environmentId` | `String` | 运行环境 ID |
| `dependencyFile` | `String` | 依赖文件路径，可选 |
| `configFile` | `String` | 配置文件路径，可选 |
| `timeoutSeconds` | `Integer` | 超时时间，`0` 表示不限制 |
| `logRef` | `String` | 日志存储引用 |
| `artifactScanRules` | `Array` | 产物扫描规则 |

`commandTemplate` 只允许以下模板变量：`taskId`、`projectId`、`datasetPath`、`modelPath`、`outputPath`、`envName`。后端必须在预检和启动前解析并保存 `resolvedCommand`。

**训练任务输入**

```json
{
  "kind": "training",
  "type": "目标检测训练",
  "name": "YOLOv8 缺陷检测复训",
  "projectId": "proj-defect",
  "datasetId": "data-defect",
  "environmentId": "env-yolo",
  "execution": {
    "source": "project_default",
    "codePath": "D:/workspace/defect-detection/code",
    "workdir": "D:/workspace/defect-detection",
    "commandTemplate": "ultralytics train --data {{datasetPath}} --project {{outputPath}}",
    "resolvedCommand": "ultralytics train --data D:/datasets/defect/data.yaml --project D:/workspace/defect-detection/runs/yolov8-retrain",
    "args": [],
    "envVars": { "TASK_ID": "task-yolo-retrain" },
    "configFile": "environment.yaml",
    "dependencyFile": "environment.yaml",
    "outputPath": "D:/workspace/defect-detection/runs/yolov8-retrain",
    "interpreterPath": "D:/DesktopAI/Environments/yolo/python.exe",
    "environmentId": "env-yolo",
    "timeoutSeconds": 0,
    "logRef": "logs/task-yolo-retrain",
    "artifactScanRules": [{ "type": "model", "pattern": "**/*.pt" }]
  },
  "parameters": {
    "epochs": 50,
    "batchSize": 16,
    "modelArchitecture": "YOLOv8s",
    "inputSize": 640,
    "optimizer": "AdamW",
    "learningRate": 0.001,
    "mixedPrecision": "AMP"
  },
  "startImmediately": false
}
```

**部署任务输入**

```json
{
  "kind": "deployment",
  "type": "LLM vLLM 推理服务",
  "name": "Qwen2.5 Adapter 推理服务",
  "projectId": "proj-sft",
  "modelAssetId": "model-qwen-adapter",
  "environmentId": "env-vllm",
  "deployment": {
    "framework": "vLLM",
    "port": 7860,
    "apiType": "OpenAI 兼容接口",
    "endpointPath": "/v1/chat/completions",
    "replicas": 1
  },
  "parameters": {
    "maxContext": 8192,
    "maxConcurrency": 8,
    "timeoutSeconds": 60,
    "temperature": 0.7,
    "quantization": "4bit"
  },
  "startImmediately": true
}
```

**输出**

```json
{
  "id": "task-yolo-retrain",
  "kind": "training",
  "status": "pending",
  "statusText": "待启动",
  "phase": null,
  "progress": { "current": 0, "total": 50, "label": "0/50" },
  "pollUrl": "/api/tasks/task-yolo-retrain",
  "logsUrl": "/api/tasks/task-yolo-retrain/logs"
}
```

### 3.5 查询任务详情
#### 1. 前置条件
+ **业务阶段**：任务详情页。
+ **前置动作**：任务已创建。

#### 2. 说明
返回任务基础信息、状态摘要、执行步骤、指标、endpoint 和按钮权限。前端任务详情页的顶部操作“复制为新任务、启动任务、停止任务、编辑任务、删除任务”必须根据 `canStart/canStop/canClone/canEdit/canDelete` 展示或禁用。

#### 3. 接口说明
**接口地址**

`GET /api/tasks/{taskId}`

**输出**

```json
{
  "id": "task-yolo-running",
  "kind": "training",
  "name": "baseline-exp-20240510-001",
  "type": "目标检测训练",
  "projectId": "proj-defect",
  "projectName": "工业缺陷检测实验",
  "datasetId": "data-defect",
  "datasetName": "工业表面缺陷数据集",
  "environmentId": "env-yolo",
  "environmentName": "3.11.8 · CUDA 12.1 / 正常",
  "status": "running",
  "statusText": "运行中",
  "phase": "training",
  "phaseText": "baseline 训练",
  "progress": { "current": 32, "total": 50, "label": "32/50" },
  "pid": 12345,
  "endpoint": null,
  "startedAt": "2026-06-20T10:48:00+08:00",
  "endedAt": null,
  "exitCode": null,
  "errorCode": null,
  "errorMessage": null,
  "stoppedReason": null,
  "latestReportId": "report-001",
  "canStart": false,
  "canStop": true,
  "canClone": true,
  "canEdit": false,
  "canDelete": true,
  "metrics": [
    { "label": "mAP50", "value": "0.892", "tone": "success" }
  ],
  "steps": [
    { "label": "数据准备与校验", "status": "已完成", "done": true }
  ]
}
```

### 3.6 查询任务配置
#### 1. 前置条件
+ **业务阶段**：任务详情 - 配置参数 Tab。
+ **前置动作**：任务已创建。

#### 2. 说明
返回任务创建时保存的不可变配置快照、基本参数和高级参数。该接口对应任务详情页“配置参数”Tab。

#### 3. 接口说明
**接口地址**

`GET /api/tasks/{taskId}/config`

**输出**

```json
{
  "taskInfo": {
    "id": "task-yolo-running",
    "name": "baseline-exp-20240510-001",
    "kind": "training",
    "type": "目标检测训练"
  },
  "execution": {
    "source": "project_default",
    "codePath": "D:/workspace/defect-detection/code",
    "workdir": "D:/workspace/defect-detection",
    "commandTemplate": "ultralytics train --data {{datasetPath}} --project {{outputPath}}",
    "resolvedCommand": "ultralytics train --data D:/datasets/defect/data.yaml --project D:/workspace/defect-detection/runs/baseline",
    "args": [],
    "envVars": { "TASK_ID": "task-yolo-running" },
    "outputPath": "D:/workspace/defect-detection/runs/baseline",
    "interpreterPath": "D:/DesktopAI/Environments/yolo/python.exe",
    "environmentId": "env-yolo",
    "dependencyFile": "environment.yaml",
    "configFile": "data.yaml",
    "timeoutSeconds": 0,
    "logRef": "logs/task-yolo-running",
    "artifactScanRules": [{ "type": "model", "pattern": "**/*.pt" }]
  },
  "basic": { "epochs": 50, "batchSize": 16, "modelArchitecture": "YOLOv8s", "inputSize": 640 },
  "advanced": { "optimizer": "AdamW", "learningRate": 0.001, "mixedPrecision": "AMP" }
}
```

### 3.7 查询任务日志
#### 1. 前置条件
+ **业务阶段**：任务详情 - 日志与诊断 Tab。
+ **前置动作**：任务已启动或已有日志。

#### 2. 说明
分页返回 stdout、stderr 和业务诊断摘要。该接口对应任务详情页“日志与诊断”Tab，并支持前端引用日志片段展示诊断证据。

#### 3. 接口说明
**接口地址**

`GET /api/tasks/{taskId}/logs`

**输入**

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `cursor` | `String` | 游标，可选 |
| `limit` | `Integer` | 行数，默认 `200` |
| `direction` | `String` | `asc` 或 `desc` |
| `keyword` | `String` | 关键词搜索 |
| `level` | `String` | `stdout`、`stderr`、`system`、`diagnosis` |
| `fromTime` | `String` | 起始时间 |
| `toTime` | `String` | 结束时间 |
| `tail` | `Boolean` | 是否读取尾部日志 |

**输出**

```json
{
  "lines": [
    {
      "id": "log-0001",
      "time": "2026-06-20T10:49:02+08:00",
      "stream": "stdout",
      "level": "info",
      "text": "Loaded 12,420 images, 6 classes."
    }
  ],
  "nextCursor": "cursor-log-0002",
  "hasMore": true,
  "truncated": false,
  "exportUrl": "/api/tasks/task-yolo-running/logs/export",
  "diagnosis": {
    "level": "warning",
    "message": "GPU 占用偏高，建议降低 batch size。",
    "evidenceLineIds": ["log-0001"]
  }
}
```

### 3.8 查询任务输出
#### 1. 前置条件
+ **业务阶段**：任务详情 - 输出结果 Tab。
+ **前置动作**：任务运行中、完成或失败。

#### 2. 说明
训练任务返回 checkpoint、模型登记入口；部署任务返回 endpoint、请求统计和测试入口。该接口对应任务详情页“输出结果”Tab。

#### 3. 接口说明
**接口地址**

`GET /api/tasks/{taskId}/outputs`

**输出**

```json
{
  "artifacts": [
    { "name": "best.pt", "path": "D:/workspace/defect/runs/best.pt", "type": "model" }
  ],
  "registeredModelId": "model-defect-best",
  "endpoint": null,
  "reportsUrl": "/api/tasks/task-yolo-running/reports"
}
```

### 3.9 生成 / 查询任务报告
#### 1. 前置条件
+ **业务阶段**：任务详情 - 日志与诊断或输出结果 Tab。
+ **前置动作**：训练完成、任务失败、推理服务运行后，用户点击“生成任务报告”。

#### 2. 说明
报告生成有独立状态，不占用任务 `phase`，不改变任务 `status`、`startedAt`、`endedAt`、`exitCode`、`errorCode`。报告生成失败只写入 `Report.errorCode` 和 `Report.errorMessage`。

#### 3. 接口说明
**接口地址**

`POST /api/tasks/{taskId}/reports`

`GET /api/tasks/{taskId}/reports`

`GET /api/reports/{reportId}`

**生成报告输入**

```json
{
  "reportType": "training_completed",
  "format": "markdown",
  "idempotencyKey": "report-task-yolo-running-001"
}
```

**报告对象**

```json
{
  "id": "report-001",
  "taskId": "task-yolo-running",
  "reportType": "training_completed",
  "status": "generating",
  "path": "D:/workspace/defect-detection/runs/baseline/reports/report-001.md",
  "errorCode": null,
  "errorMessage": null,
  "createdAt": "2026-07-02T15:50:00+08:00",
  "updatedAt": "2026-07-02T15:50:00+08:00"
}
```

`reportType` 可选值：`training_completed`、`failure_diagnosis`、`deployment_runtime`。

`Report.status` 可选值：`pending`、`generating`、`generated`、`generate_failed`。

### 3.10 启动任务
#### 1. 前置条件
+ **业务阶段**：任务操作。
+ **前置动作**：任务状态为 `pending`，且预检通过。

#### 2. 说明
启动训练进程或部署服务进程。启动会占用本机资源并创建进程，属于高风险操作，必须传入 `confirmed: true`。

#### 3. 接口说明
**接口地址**

`POST /api/tasks/{taskId}/start`

支持 `Idempotency-Key` 请求头或请求体 `idempotencyKey`。

**输入**

```json
{
  "confirmed": true,
  "idempotencyKey": "start-task-yolo-retrain-001"
}
```

**输出**

```json
{
  "id": "task-yolo-retrain",
  "status": "running",
  "statusText": "运行中",
  "phase": "startup",
  "phaseText": "启动中",
  "pid": 12345,
  "startedAt": "2026-07-02T15:55:00+08:00",
  "canStart": false,
  "canStop": true
}
```

### 3.11 停止任务
#### 1. 前置条件
+ **业务阶段**：任务操作。
+ **前置动作**：任务正在运行；用户确认停止。

#### 2. 说明
高风险操作。停止训练或部署服务。

#### 3. 接口说明
**接口地址**

`POST /api/tasks/{taskId}/stop`

支持 `Idempotency-Key` 请求头或请求体 `idempotencyKey`。

**输入**

```json
{
  "confirmed": true,
  "idempotencyKey": "stop-task-yolo-running-001"
}
```

**输出**

```json
{
  "id": "task-yolo-running",
  "status": "stopped",
  "statusText": "已停止",
  "phase": null,
  "stoppedReason": "USER_STOPPED",
  "endedAt": "2026-07-02T16:00:00+08:00",
  "canStart": false,
  "canStop": false,
  "canClone": true
}
```

### 3.12 删除任务
#### 1. 前置条件
+ **业务阶段**：任务详情或任务列表删除任务。
+ **前置动作**：用户确认删除任务记录。

#### 2. 说明
高风险操作。默认只删除任务登记记录、日志索引和前端可见记录，不删除数据集、模型资产或本机输出文件。`deleteLocalFiles` 在 MVP 阶段只允许为 `false`。

#### 3. 接口说明
**接口地址**

`DELETE /api/tasks/{taskId}`

**输入**

```json
{
  "confirmed": true,
  "deleteLocalFiles": false
}
```

**输出**

```json
{
  "deleted": true,
  "taskId": "task-yolo-running"
}
```

### 3.13 复制 / 重试任务
#### 1. 前置条件
+ **业务阶段**：任务操作。
+ **前置动作**：用户从已有任务发起复制或重试。

#### 2. 说明
复制或重试不会直接启动任务，而是生成任务草稿。

#### 3. 接口说明
**接口地址**

`POST /api/tasks/{taskId}/clone`

`POST /api/tasks/{taskId}/retry`

**输出**

```json
{
  "draft": {
    "sourceTaskId": "task-yolo-failed",
    "kind": "training",
    "type": "目标检测训练",
    "parameters": {}
  }
}
```

### 3.14 登记训练产物为模型
#### 1. 前置条件
+ **业务阶段**：训练任务输出结果。
+ **前置动作**：训练任务已完成或已有可用 checkpoint。

#### 2. 说明
将训练产物登记为模型资产。

#### 3. 接口说明
**接口地址**

`POST /api/tasks/{taskId}/register-model`

**输入**

```json
{
  "artifactPath": "D:/workspace/defect/runs/best.pt",
  "name": "yolov8s-defect-best.pt",
  "type": "目标检测模型"
}
```

**输出**

```json
{
  "modelId": "model-defect-best"
}
```

### 3.15 查询部署测试规格
#### 1. 前置条件
+ **业务阶段**：部署测试页。
+ **前置动作**：部署任务已创建。

#### 2. 说明
根据部署任务类型返回测试表单字段。

#### 3. 接口说明
**接口地址**

`GET /api/tasks/{taskId}/test-spec`

**输出**

```json
{
  "endpoint": "http://127.0.0.1:7860/v1/chat/completions",
  "method": "POST",
  "fields": [
    { "name": "messages", "type": "textarea", "required": true },
    { "name": "temperature", "type": "number", "default": 0.7 }
  ]
}
```

### 3.16 测试部署任务
#### 1. 前置条件
+ **业务阶段**：部署测试页。
+ **前置动作**：部署任务运行中。

#### 2. 说明
调用本地部署服务并返回响应。

#### 3. 接口说明
**接口地址**

`POST /api/tasks/{taskId}/test`

**输入**

```json
{
  "payload": {
    "messages": [{ "role": "user", "content": "你好" }],
    "temperature": 0.7
  }
}
```

**输出**

```json
{
  "statusCode": 200,
  "latencyMs": 728,
  "response": {}
}
```

## 4. 数据
### 4.1 查询数据集列表
#### 1. 前置条件
+ **业务阶段**：数据列表页。
+ **前置动作**：系统中已有数据集资产或用户准备导入。

#### 2. 说明
支持搜索、类型筛选、状态筛选和分页。

#### 3. 接口说明
**接口地址**

`GET /api/datasets`

**输入**

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `keyword` | `String` | 名称、路径、类型搜索 |
| `type` | `String` | 数据集类型 |
| `status` | `String` | 状态 |
| `projectId` | `String` | 关联项目 |
| `page` | `Integer` | 页码 |
| `pageSize` | `Integer` | 每页数量 |


**输出**

```json
{
  "items": [
    {
      "id": "data-defect",
      "name": "工业表面缺陷数据集",
      "type": "目标检测",
      "path": "D:/datasets/defect",
      "format": "YOLO",
      "sampleCount": 12480,
      "status": "存在警告"
    }
  ],
  "total": 1
}
```

### 4.2 导入数据集
#### 1. 前置条件
+ **业务阶段**：导入数据集弹窗。
+ **前置动作**：用户选择本地路径并填写类型、格式、标注文件。

#### 2. 说明
导入数据集登记，支持导入后自动检查。

#### 3. 接口说明
**接口地址**

`POST /api/datasets`

**输入**

```json
{
  "name": "工业表面缺陷数据集",
  "type": "目标检测",
  "path": "D:/datasets/defect",
  "format": "YOLO",
  "annotation": "data.yaml",
  "projectIds": ["proj-defect"],
  "autoCheck": true
}
```

**输出**

```json
{
  "id": "data-defect",
  "status": "检查中"
}
```

### 4.3 查询数据集详情
#### 1. 前置条件
+ **业务阶段**：数据详情页。
+ **前置动作**：数据集已导入。

#### 2. 说明
返回数据集基础信息、绑定项目、检查摘要。

#### 3. 接口说明
**接口地址**

`GET /api/datasets/{datasetId}`

**输出**

返回数据集详情对象。

### 4.4 查询数据结构画像
#### 1. 前置条件
+ **业务阶段**：数据详情 - 结构信息。
+ **前置动作**：数据集路径可访问。

#### 2. 说明
根据数据格式返回目录结构和字段说明。

#### 3. 接口说明
**接口地址**

`GET /api/datasets/{datasetId}/profile`

**输出**

```json
{
  "structureTitle": "YOLO 数据结构",
  "structure": ["images/train", "images/val", "labels/train", "labels/val", "data.yaml"],
  "checkScope": "检查图片与 label 是否一一对应、类别分布和 data.yaml 可读性。"
}
```

### 4.5 检查数据集
#### 1. 前置条件
+ **业务阶段**：数据详情或导入后自动检查。
+ **前置动作**：数据集路径存在。

#### 2. 说明
检查图片、标注、字段、类别分布、样本合法性。输出复用 `CheckResult`。

#### 3. 接口说明
**接口地址**

`POST /api/datasets/{datasetId}/check`

**输出**

```json
{
  "status": "warning",
  "summary": "数据集存在非阻断警告",
  "items": [
    { "name": "标签完整性", "status": "warning", "severity": "warning", "code": "LABEL_FILE_MISSING", "message": "3 个标签文件缺失", "suggestion": "补齐标签或排除样本", "blocking": false, "value": 3 },
    { "name": "类别分布", "status": "warning", "severity": "warning", "code": "CLASS_DISTRIBUTION_SKEW", "message": "val 集合类别分布偏斜", "suggestion": "重新划分训练/验证集", "blocking": false, "value": null }
  ],
  "checkedAt": "2026-07-02T15:20:00+08:00",
  "source": "dataset",
  "warnings": ["3 个标签文件缺失", "val 集合类别分布偏斜"]
}
```

### 4.6 查询数据集检查结果
#### 1. 前置条件
+ **业务阶段**：数据详情 - AI 数据检查摘要。
+ **前置动作**：数据集已检查。

#### 2. 说明
返回最近一次检查结果，输出复用 `CheckResult`。

#### 3. 接口说明
**接口地址**

`GET /api/datasets/{datasetId}/check-result`

**输出**

返回检查结果对象。

### 4.7 更新数据集绑定项目
#### 1. 前置条件
+ **业务阶段**：数据列表或数据详情绑定项目。
+ **前置动作**：用户选择项目。

#### 2. 说明
一个数据集可以绑定多个项目。

#### 3. 接口说明
**接口地址**

`PATCH /api/datasets/{datasetId}/projects`

**输入**

```json
{
  "projectIds": ["proj-defect"]
}
```

**输出**

返回更新后的数据集对象。

### 4.8 删除数据集
#### 1. 前置条件
+ **业务阶段**：数据列表删除数据集。
+ **前置动作**：用户已确认删除。

#### 2. 说明
默认只删除登记记录，不删除本地数据文件。`deleteLocalFiles` 在 MVP 阶段只允许为 `false`。

#### 3. 接口说明
**接口地址**

`DELETE /api/datasets/{datasetId}`

**输入**

```json
{
  "confirmed": true,
  "deleteLocalFiles": false
}
```

**输出**

```json
{
  "deleted": true
}
```

## 5. 模型
### 5.1 查询模型列表
#### 1. 前置条件
+ **业务阶段**：模型资产列表页。
+ **前置动作**：系统中已有模型资产或用户准备导入。

#### 2. 说明
支持搜索、模型类型筛选、状态筛选和分页。

#### 3. 接口说明
**接口地址**

`GET /api/models`

**输入**

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `keyword` | `String` | 名称、路径、类型、项目搜索 |
| `type` | `String` | 模型类型 |
| `status` | `String` | 状态 |
| `projectId` | `String` | 关联项目 |
| `compatibleWith` | `String` | 可选，按推理服务类型筛选兼容模型，例如 `LLM vLLM 推理服务` |
| `page` | `Integer` | 页码 |
| `pageSize` | `Integer` | 每页数量 |


**输出**

```json
{
  "items": [
    {
      "id": "model-defect-best",
      "name": "yolov8s-defect-best.pt",
      "type": "目标检测模型",
      "source": "训练产物",
      "path": "D:/models/yolov8s-defect-best.pt",
      "format": ".pt",
      "status": "可用"
    }
  ],
  "total": 1
}
```

### 5.2 导入模型
#### 1. 前置条件
+ **业务阶段**：导入模型弹窗。
+ **前置动作**：用户选择本地模型文件或目录。

#### 2. 说明
导入模型资产登记，支持导入后自动检查。

#### 3. 接口说明
**接口地址**

`POST /api/models`

**输入**

```json
{
  "name": "yolov8s-defect-best.pt",
  "type": "目标检测模型",
  "source": "外部导入",
  "path": "D:/models/yolov8s-defect-best.pt",
  "format": ".pt",
  "framework": "Ultralytics / PyTorch",
  "precision": "FP32",
  "projectIds": ["proj-defect"],
  "recommendedEnvironmentId": "env-yolo",
  "autoCheck": true
}
```

**输出**

```json
{
  "id": "model-defect-best",
  "status": "检查中"
}
```

### 5.3 查询模型详情
#### 1. 前置条件
+ **业务阶段**：模型详情页。
+ **前置动作**：模型资产已存在。

#### 2. 说明
返回模型基础信息、来源任务、关联项目和部署建议。

#### 3. 接口说明
**接口地址**

`GET /api/models/{modelId}`

**输出**

返回模型详情对象。

### 5.4 检查模型
#### 1. 前置条件
+ **业务阶段**：模型导入后或手动检查。
+ **前置动作**：模型路径存在。

#### 2. 说明
检查模型文件、配置、tokenizer、adapter、推荐环境和部署兼容性。输出复用 `CheckResult`。

#### 3. 接口说明
**接口地址**

`POST /api/models/{modelId}/check`

**输出**

```json
{
  "status": "pass",
  "summary": "权重文件完整，可直接创建 CV 推理服务",
  "items": [
    { "name": "模型文件", "status": "pass", "severity": "info", "code": "MODEL_FILE_OK", "message": "模型文件完整", "suggestion": "", "blocking": false, "value": "best.pt" }
  ],
  "checkedAt": "2026-07-02T15:30:00+08:00",
  "source": "model",
  "warnings": []
}
```

### 5.5 查询模型检查结果
#### 1. 前置条件
+ **业务阶段**：模型详情 - 检查与部署建议。
+ **前置动作**：模型已检查。

#### 2. 说明
返回最近一次模型检查结果，输出复用 `CheckResult`。

#### 3. 接口说明
**接口地址**

`GET /api/models/{modelId}/check-result`

**输出**

返回模型检查结果对象。

### 5.6 更新模型绑定项目
#### 1. 前置条件
+ **业务阶段**：模型列表或模型详情绑定项目。
+ **前置动作**：用户选择项目。

#### 2. 说明
一个模型可以绑定多个项目。

#### 3. 接口说明
**接口地址**

`PATCH /api/models/{modelId}/projects`

**输入**

```json
{
  "projectIds": ["proj-defect"]
}
```

**输出**

返回更新后的模型对象。

### 5.7 删除模型
#### 1. 前置条件
+ **业务阶段**：模型列表删除模型。
+ **前置动作**：用户已确认删除。

#### 2. 说明
默认只删除登记记录，不删除本地模型文件。`deleteLocalFiles` 在 MVP 阶段只允许为 `false`。

#### 3. 接口说明
**接口地址**

`DELETE /api/models/{modelId}`

**输入**

```json
{
  "confirmed": true,
  "deleteLocalFiles": false
}
```

**输出**

```json
{
  "deleted": true
}
```

## 6. 设置 - 运行环境
### 6.1 查询环境列表
#### 1. 前置条件
+ **业务阶段**：设置 - 环境创建 Tab。
+ **前置动作**：系统已初始化运行环境配置。

#### 2. 说明
展示系统创建、本地导入、自定义创建的环境。

#### 3. 接口说明
**接口地址**

`GET /api/environments`

**输入**

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `keyword` | `String` | 环境名称、用途、框架、CUDA 搜索 |
| `purpose` | `String` | 环境用途 |
| `status` | `String` | 状态 |
| `page` | `Integer` | 页码 |
| `pageSize` | `Integer` | 每页数量 |


**输出**

返回分页环境列表。

### 6.2 查询环境详情
#### 1. 前置条件
+ **业务阶段**：环境详情抽屉。
+ **前置动作**：用户选择环境。

#### 2. 说明
返回 Python、框架、CUDA、检测结果、最近项目。

#### 3. 接口说明
**接口地址**

`GET /api/environments/{envId}`

**输出**

返回环境详情对象。

### 6.3 创建环境
#### 1. 前置条件
+ **业务阶段**：创建系统环境或自定义环境。
+ **前置动作**：用户填写环境创建表单。

#### 2. 说明
支持系统模板创建和自定义依赖创建。

#### 3. 接口说明
**接口地址**

`POST /api/environments`

**输入**

```json
{
  "mode": "create",
  "name": "YOLO 系列训练环境",
  "purpose": "训练",
  "template": "PyTorch + Ultralytics",
  "python": "3.11.8",
  "cuda": "CUDA 12.1",
  "savePath": "D:/DesktopAI/Environments/yolo",
  "autoCheck": true
}
```

**输出**

返回新环境对象。

### 6.4 导入本地环境
#### 1. 前置条件
+ **业务阶段**：导入本地环境。
+ **前置动作**：用户选择已有 Conda / venv。

#### 2. 说明
登记本地环境，不强制重新创建。

#### 3. 接口说明
**接口地址**

`POST /api/environments/import`

**输入**

```json
{
  "name": "本地 QLoRA 环境",
  "pythonPath": "D:/miniconda3/envs/qlora/python.exe",
  "purpose": "LLM 微调",
  "autoCheck": true
}
```

**输出**

返回导入后的环境对象。

### 6.5 检测环境
#### 1. 前置条件
+ **业务阶段**：环境检测。
+ **前置动作**：环境已创建或导入。

#### 2. 说明
检测 Python、框架版本、CUDA、依赖和任务兼容性。输出复用 `CheckResult`。

#### 3. 接口说明
**接口地址**

`POST /api/environments/{envId}/check`

**输出**

```json
{
  "status": "pass",
  "summary": "运行环境可用",
  "items": [
    { "name": "Python", "status": "pass", "severity": "info", "code": "PYTHON_OK", "message": "Python 可用", "suggestion": "", "blocking": false, "value": "3.11.8" },
    { "name": "CUDA", "status": "pass", "severity": "info", "code": "CUDA_OK", "message": "CUDA 可用", "suggestion": "", "blocking": false, "value": "12.1" }
  ],
  "checkedAt": "2026-07-02T15:25:00+08:00",
  "source": "environment",
  "warnings": []
}
```

### 6.6 批量检测环境
#### 1. 前置条件
+ **业务阶段**：设置 - 环境创建 Tab。
+ **前置动作**：用户点击“检测全部环境”。

#### 2. 说明
检测全部运行环境，检查 Python、包管理器、框架版本、CUDA、驱动、磁盘、内存、依赖和任务兼容性。该接口不安装依赖、不修复环境、不删除文件，属于低风险读检查操作。

后端可以同步返回快速检查结果，也可以返回 `jobId` 后异步执行。前端根据 `jobId` 轮询 `GET /api/environments/batch-check/{jobId}` 刷新批量检测进度。

#### 3. 接口说明
**接口地址**

`POST /api/environments/batch-check`

`GET /api/environments/batch-check/{jobId}`

**输入**

```json
{
  "purpose": "",
  "includeUnavailable": true
}
```

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `purpose` | `String` | 可选，按环境用途筛选 |
| `includeUnavailable` | `Boolean` | 是否包含已不可用环境 |

**输出**

```json
{
  "jobId": "env-check-job-001",
  "status": "running",
  "total": 3,
  "checked": 1,
  "items": [
    {
      "environmentId": "env-yolo",
      "environmentName": "YOLO 系列训练环境",
      "checkResult": {
        "status": "pass",
        "summary": "运行环境可用",
        "items": [
          { "name": "Python", "status": "pass", "severity": "info", "code": "PYTHON_OK", "message": "Python 可用", "suggestion": "", "blocking": false, "value": "3.11.8" },
          { "name": "CUDA", "status": "pass", "severity": "info", "code": "CUDA_OK", "message": "CUDA 可用", "suggestion": "", "blocking": false, "value": "12.1" }
        ],
        "checkedAt": "2026-07-02T15:25:00+08:00",
        "source": "environment",
        "warnings": []
      }
    }
  ],
  "failedEnvironmentIds": []
}
```

`status` 可选值：`pending`、`running`、`completed`、`partial_failed`、`failed`。

### 6.7 修复环境
#### 1. 前置条件
+ **业务阶段**：环境修复。
+ **前置动作**：环境状态为需修复或检测失败；用户确认修复。

#### 2. 说明
高风险操作。可能安装、升级或替换依赖。

#### 3. 接口说明
**接口地址**

`POST /api/environments/{envId}/repair`

**输入**

```json
{
  "confirmed": true,
  "strategy": "auto"
}
```

**输出**

```json
{
  "repairTaskId": "repair-env-yolo-001",
  "repairStatus": "running"
}
```

### 6.8 删除环境
#### 1. 前置条件
+ **业务阶段**：环境删除。
+ **前置动作**：用户确认删除。

#### 2. 说明
默认只删除登记记录，不删除本地环境目录。`deleteLocalFiles` 在 MVP 阶段只允许为 `false`。

#### 3. 接口说明
**接口地址**

`DELETE /api/environments/{envId}`

**输入**

```json
{
  "confirmed": true,
  "deleteLocalFiles": false
}
```

**输出**

```json
{
  "deleted": true
}
```

## 7. 设置 - 个人信息与偏好
### 7.1 查询个人信息
#### 1. 前置条件
+ **业务阶段**：设置 - 个人信息 Tab。
+ **前置动作**：用户已登录或处于本地单用户模式。

#### 2. 说明
返回账号 ID、邮箱、手机号等资料。

#### 3. 接口说明
**接口地址**

`GET /api/settings/profile`

**输出**

```json
{
  "accountId": "admin",
  "email": "admin@example.com",
  "phone": "13800138000"
}
```

### 7.2 保存个人信息
#### 1. 前置条件
+ **业务阶段**：保存个人信息。
+ **前置动作**：用户修改账号资料。

#### 2. 说明
校验邮箱、手机号格式。

#### 3. 接口说明
**接口地址**

`PATCH /api/settings/profile`

**输入**

```json
{
  "accountId": "admin",
  "email": "admin@example.com",
  "phone": "13800138000"
}
```

**输出**

返回更新后的个人信息。

### 7.3 查询偏好配置
#### 1. 前置条件
+ **业务阶段**：设置 - 偏好配置。
+ **前置动作**：用户进入设置页。

#### 2. 说明
返回主题、语言、默认路径、确认策略等配置。

#### 3. 接口说明
**接口地址**

`GET /api/settings/preferences`

**输出**

```json
{
  "theme": "system",
  "language": "zh-CN",
  "defaultWorkspace": "D:/workspace",
  "confirmPolicy": "high_risk_only"
}
```

### 7.4 保存偏好配置
#### 1. 前置条件
+ **业务阶段**：保存偏好配置。
+ **前置动作**：用户修改偏好配置。

#### 2. 说明
保存本地桌面端偏好。

#### 3. 接口说明
**接口地址**

`PATCH /api/settings/preferences`

**输入**

```json
{
  "theme": "system",
  "language": "zh-CN",
  "defaultWorkspace": "D:/workspace",
  "confirmPolicy": "high_risk_only"
}
```

**输出**

返回更新后的偏好配置。

## 8. 系统文件与路径能力
### 8.1 选择本地目录
#### 1. 前置条件
+ **业务阶段**：创建项目、导入数据、导入模型、创建环境。
+ **前置动作**：前端需要打开本地目录选择器。

#### 2. 说明
桌面端场景下可由前端调用系统能力，也可由后端暴露统一入口。

#### 3. 接口说明
**接口地址**

`POST /api/local-files/pick-directory`

**输入**

```json
{
  "title": "选择项目工作区",
  "defaultPath": "D:/workspace"
}
```

**输出**

```json
{
  "path": "D:/workspace/defect-detection"
}
```

### 8.2 校验本地路径
#### 1. 前置条件
+ **业务阶段**：所有涉及本地路径的表单。
+ **前置动作**：用户填写或选择路径。

#### 2. 说明
校验路径是否存在、是否在允许范围、是否可读写。

#### 3. 接口说明
**接口地址**

`POST /api/local-files/validate-path`

**输入**

```json
{
  "path": "D:/datasets/defect",
  "expect": "directory",
  "permission": "read"
}
```

**输出**

```json
{
  "ok": true,
  "exists": true,
  "type": "directory",
  "allowed": true
}
```

## 9. 后端 Adapter 约定
### 9.1 任务 Adapter 映射
#### 1. 前置条件
+ **业务阶段**：后端任务执行。
+ **前置动作**：任务已通过预检并准备启动。

#### 2. 说明
后端根据 `kind` 和 `type` 选择 Adapter。

#### 3. 接口说明
| `kind` | `type` | 后端 Adapter |
| :--- | :--- | :--- |
| `training` | 图像分类训练 | Image Classification Adapter |
| `training` | 目标检测训练 | Detection Adapter |
| `training` | 语义分割训练 | Segmentation Adapter |
| `training` | 自定义深度学习训练 | Custom Training Adapter |
| `training` | LLM 微调 | LLaMA-Factory / Transformers Adapter |
| `training` | Embedding 微调 | Sentence-Transformers Adapter |
| `training` | Rerank 微调 | CrossEncoder Adapter |
| `deployment` | CV 推理服务 | CV Inference Adapter |
| `deployment` | LLM vLLM 推理服务 | vLLM Adapter |
| `deployment` | Embedding 推理服务 | Embedding Service Adapter |
| `deployment` | Rerank 推理服务 | Rerank Service Adapter |
| `deployment` | 自定义推理服务 | Custom Deployment Adapter |


### 9.2 目标检测数据处理要求
#### 1. 前置条件
+ **业务阶段**：目标检测训练任务。
+ **前置动作**：用户选择 COCO 或 YOLO 数据集。

#### 2. 说明
目标检测训练必须在训练前和增强后过滤无效 bbox，避免出现零宽/零高框导致训练中断。

#### 3. 接口说明
| 规则 | 说明 |
| :--- | :--- |
| bbox 宽度校验 | `x_max <= x_min` 的框必须过滤 |
| bbox 高度校验 | `y_max <= y_min` 的框必须过滤 |
| COCO 输入 | 使用 `[x, y, width, height]` |
| YOLO 输入 | 使用归一化 `[cx, cy, w, h]` 并转换 |
| 增强后过滤 | 随机裁剪、透视、缩放后必须再次过滤无效框 |
| 空标注处理 | 某张图所有框被过滤时，按后端策略跳过或保留空标注 |


### 9.3 任务状态
#### 1. 前置条件
+ **业务阶段**：任务生命周期管理。
+ **前置动作**：任务已创建。

#### 2. 说明
训练任务和部署任务统一维护 `status` 主状态和 `phase` 执行阶段。`status` 是任务生命周期唯一主状态；`phase` 只表示当前执行步骤，不替代主状态。

任务主状态不包含 `unavailable`。数据集、模型、运行环境的不可用属于资源状态；任务侧通过预检 `blockingReasons`、`errorCode` 和 `errorMessage` 表达阻断原因。

#### 3. 接口说明
**任务主状态**

| `apiStatus` | 中文展示 | PRD 状态 | 适用 kind | 是否终态 | 允许操作 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `draft` | 等待配置 | 等待配置 | training / deployment | 否 | 编辑、删除 |
| `pending` | 待启动 | 待启动 | training / deployment | 否 | 启动、编辑、复制、删除 |
| `checking` | 检查中 | 检查中 | training / deployment | 否 | 查看、停止检查 |
| `queued` | 排队中 | 排队中 | training / deployment | 否 | 查看、停止 |
| `running` | 运行中 | 运行中 | training / deployment | 否 | 停止、复制 |
| `stopping` | 停止中 | 停止中 | training / deployment | 否 | 查看 |
| `stopped` | 已停止 | 已停止 | training / deployment | 是 | 复制、删除 |
| `completed` | 已完成 | 已完成 | training | 是 | 复制、生成报告、登记模型、删除 |
| `start_failed` | 启动失败 | 启动失败 | training / deployment | 是 | 编辑、复制、删除、AI 诊断 |
| `failed` | 失败 | 失败 | training / deployment | 是 | 复制、生成失败诊断报告、删除、AI 诊断 |
| `error` | 异常 | 异常 | training / deployment | 是 | 复制、删除、AI 诊断 |

**任务执行阶段 `phase`**

| `phase` | 前端展示 | 适用 kind | 说明 |
| :--- | :--- | :--- | :--- |
| `environment_check` | 环境检查中 | training / deployment | 检查 Python、依赖、CUDA |
| `dataset_check` | 数据检查中 | training | 检查训练数据 |
| `model_check` | 模型检查中 | deployment | 检查模型文件、tokenizer、adapter |
| `resource_check` | 资源检查中 | training / deployment | 检查 GPU、内存、磁盘、端口 |
| `queue` | 排队中 | training / deployment | 等待执行资源 |
| `startup` | 启动中 | training / deployment | 创建训练进程或服务进程 |
| `model_loading` | 模型加载中 | deployment | 推理服务加载模型 |
| `service_start` | 服务启动中 | deployment | 推理服务启动 HTTP endpoint |
| `health_check` | 健康检查中 | deployment | 检查服务 endpoint |
| `training` | 训练中 | training | 训练主过程 |
| `validation` | 验证评估中 | training | 验证集评估 |
| `artifact_scan` | 导出产物中 | training | 扫描 checkpoint、报告候选和模型文件 |

报告生成不属于任务 `phase`。报告状态独立维护在 `Report.status` 中。

**训练任务状态规则**

1. 创建后为 `pending`；若表单未完成可为 `draft`。
2. 启动后依次进入 `checking`、`queued`、`running`。
3. 训练正常退出并完成产物扫描后进入 `completed`。
4. 启动前失败进入 `start_failed`。
5. 训练进程非 0 退出进入 `failed`。
6. 未知执行器异常、状态同步异常或进程丢失进入 `error`。
7. 用户停止后进入 `stopping`，进程结束后进入 `stopped`，`stoppedReason=USER_STOPPED`。

**部署任务状态规则**

1. 创建后为 `pending`；若表单未完成可为 `draft`。
2. 启动后依次进入 `checking`、`queued`、`running`。
3. 健康检查通过后保持 `running`，不会进入 `completed`。
4. 端口冲突、模型缺文件、基础模型不匹配、依赖缺失等启动前问题进入 `start_failed`。
5. 服务进程启动后崩溃进入 `error`。
6. 用户停止后进入 `stopping`，进程结束后进入 `stopped`。

**错误字段**

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| `errorCode` | `String` | 结构化错误码，例如 `COMMAND_NOT_FOUND`、`CUDA_OOM`、`PORT_IN_USE` |
| `errorMessage` | `String` | 错误摘要 |
| `exitCode` | `Integer` | 进程退出码 |
| `stoppedReason` | `String` | `USER_STOPPED`、`PROCESS_EXITED`、`SYSTEM_SHUTDOWN` |
