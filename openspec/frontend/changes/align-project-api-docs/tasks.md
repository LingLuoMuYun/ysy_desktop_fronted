# Tasks

- [x] 对齐项目 API 替换清单与新版后端项目接口。
- [x] 删除旧版项目创建字段说明：`codeSource`、`codePath`、`datasetIds`。
- [x] 补充 `status` / `statusText` 分离、创建确认和默认环境展示约束。
- [x] 更新后端接口索引中的项目接口数量与编号。
- [x] 标记 `PATCH /api/projects/{projectId}` 为待后端确认。
