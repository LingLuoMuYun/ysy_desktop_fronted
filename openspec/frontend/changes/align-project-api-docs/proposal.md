# Align Project API Docs

## 背景

后端更新了项目模块接口文档，项目创建、项目规格、项目状态和删除接口编号发生变化。前端接口替换文档需要同步，避免后续按旧字段或未确认接口接入。

## 范围

- 更新前端接口替换文档的项目章节。
- 对齐项目列表、规格、创建、详情和删除接口字段。
- 标记新版后端文档未列出的项目编辑接口为待确认。

## 非范围

- 不实现真实项目 API 调用。
- 不修改项目页组件、类型或 mock 数据。
- 不验证后端真实服务。
