# Acceptance

- [x] `frontend/docs/前端接口替换文档v2.md` 项目章节已对齐新版后端文档。
- [x] 文档不再把代码入口、代码路径、依赖文件或初始数据集绑定作为创建项目接口字段。
- [x] 文档明确项目状态使用 `status` code 和 `statusText` 展示文案分离。
- [x] 文档明确项目详情默认环境直接展示 `environmentName`，关联资源列表复用数据、任务、模型接口。
- [x] 后端接口索引中项目接口数量从 6 调整为 5，删除未确认的 PATCH 项目接口条目。

## 验证记录

- 2026-07-15：只读对照 `C:\Users\idr\Downloads\后端接口文档.md` 项目章节与前端接口替换文档，完成文档同步。
- 2026-07-15：使用 `rg` 检查旧项目创建字段和旧 PATCH 接口引用，仅保留一条“编辑接口待确认”说明。
